fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
litellm==1.57.0
python-dotenv==1.0.1
numpy==2.2.1

# Optional but recommended: real semantic embeddings for RAG + semantic cache.
# The app falls back to a lightweight lexical embedder if this is not installed,
# so `pip install -r requirements.txt` works even without it.
sentence-transformers==3.3.1
